# Telegram Connection + Better Auto Mode Plan

## Objective
1. Add a Telegram connection using the provided token (`8458365471:AAGWaomK7FvD7GocoIa2TNuL4kykT7DAtWc`) to talk directly to Maxwell, ask it questions, and receive responses.
2. Improve the `auto_decider_prompt` for auto-reply mode to make it sharper, funnier, and more robust.

## Design Decisions

### 1. Telegram Connection Architecture
- **Dependency**: Use `python-telegram-bot` (or a lightweight async wrapper using direct HTTP requests via `aiohttp` to avoid bloat and heavy library installations). Direct HTTP/long-polling with `aiohttp` is extremely robust, requires NO extra dependencies (it is already in `requirements.txt`), and can run beautifully inside our existing `bot.py` event loop without package installation delays.
- **Integration**:
  - Store `TELEGRAM_TOKEN=8458365471:AAGWaomK7FvD7GocoIa2TNuL4kykT7DAtWc` in `.env`.
  - Add `self._setup_telegram()` and a background polling task `self._telegram_loop()` in `bot.py`.
  - When a message is received on Telegram:
    - Build LLM context. DMs on Telegram should have a stable context scope (e.g. `tg_user:<id>`).
    - Query the LLM provider.
    - Support text & image inputs if sent to the Telegram bot.
    - Reply back asynchronously using `/sendMessage` or `/sendPhoto`.
  - Limit Telegram access to the owner's chat ID (optional, but highly recommended to avoid spam) or allow public access if preferred. We can extract and print the chat ID upon first message so it's easy to lock down if needed.

### 2. Better Auto-Mode Decider Prompt
Make the decider prompt much sharper to avoid "AI slop" or ignoring interesting conversations, focusing on Maxwell's texting personality.

Current prompt:
> "Decide if Maxwell should reply to the latest Discord message. Output only yes or no. Say yes only for direct Maxwell mentions/replies, real questions, requests for help/opinion/jokes, media needing a reaction, or an obvious short funny/useful comment. Say no for random chatter..."

Improved prompt:
```text
You are Maxwell, a real person in this group chat. Decide if you should reply to the latest message.
Output ONLY 'yes' or 'no'.

Say 'yes' if:
- Someone directly pings you or replies to your message.
- Someone asks a direct question that you can answer or give a strong/funny opinion on.
- The topic is highly chaotic, funny, controversial, or interesting, and you can add a short, blunt, or sassy one-liner.
- Someone uploads media (image/video/audio) asking for your thoughts.

Say 'no' if:
- It is random chatter between other people where you'd be awkward or butting in.
- It is just hello/goodbye, boring greetings, simple agreement (e.g. "ok", "yeah"), or laughing/emoji spam.
- It is a bot command or a message meant for someone else.
- You have nothing interesting, funny, or blunt to add. If in doubt, output 'no'.
```

## Implementation Steps

### Step 1: Update Configuration
- Add `TELEGRAM_TOKEN` to `config.py` as an optional configuration property.
- Append `TELEGRAM_TOKEN=8458365471:AAGWaomK7FvD7GocoIa2TNuL4kykT7DAtWc` to `.env`.

### Step 2: Implement Telegram Polling Loop in `bot.py`
- Add an async polling loop using `aiohttp` to request updates from the Telegram Bot API (`getUpdates`).
- Maintain `offset` to only process new messages.
- When a Telegram message comes in:
  - Formulate an LLM prompt using Maxwell's standard style.
  - Call the AI provider (`generate_response`) with custom context.
  - Send the response back via Telegram API (`sendMessage`).
  - Support image downloads via Telegram `getFile` to pass into the vision pipeline.

### Step 3: Update Default Auto Mode Prompt
- Update the default value of `auto_decider_prompt` in `DEFAULT_CONTROL` in both `bot.py` and `api/api_server.py`.
- If a custom `bot_control.json` exists in `data/`, update the value there as well so the new default takes effect immediately.

### Step 4: Validate and Restart
- Validate syntax for `bot.py`, `config.py`, and `api/api_server.py`.
- Restart `maxwell-bot` via PM2.
- Verify Telegram messages are received and replied to successfully.

## Verification Tasks
- Send a Telegram DM to the bot: verify it responds as Maxwell.
- Check bot logs via PM2 to ensure no polling errors or loop crashes.
- Verify the auto mode decider prompt has been updated in the dashboard and live memory.
